{
  "name": "better-dex",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/generate-data.mjs --skip-if-exists",
    "dev": "vite",
    "prebuild": "node scripts/generate-data.mjs",
    "build": "tsc -b && vite build && node scripts/generate-sw.mjs",
    "preview": "vite preview",
    "data": "node scripts/generate-data.mjs --force"
  },
  "dependencies": {
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-router-dom": "^7.6.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5",
    "vite": "^7.0.0"
  }
}
